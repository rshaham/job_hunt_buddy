# Job Hunt Buddy Environment Variables
# Copy this file to .env.local and fill in your values

# ============================================
# CLIENT-SIDE (Browser)
# ============================================
# Note: The Claude API key is stored in the app settings (localStorage)
# and not in environment variables for this local-only application.

# ============================================
# SERVER-SIDE (Vercel API Routes)
# ============================================
# These are used by the API proxy routes and should be set in Vercel Dashboard
# for production. For local development, use .env.local with `vercel dev`.

# Tavily API Key for web search
# Get your free API key at https://tavily.com (1000 searches/month free)
TAVILY_API_KEY=

# SerApi API Key for job listings search
# Get your API key at https://serpapi.com
SERPAPI_API_KEY=

# Upstash Redis for rate limiting (optional but recommended)
# Get free credentials at https://upstash.com
# Without these, rate limiting is disabled (falls open)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Rate limiting configuration (optional)
# RATE_LIMIT_PER_HOUR=20      # Requests per IP per hour (default: 20)
# DAILY_GLOBAL_CAP=500        # Total requests per day across all users (default: 500)

# ============================================
# FEATURE FLAGS (Client-side)
# ============================================
# Control visibility of features that use external services.
# Set in Vercel Dashboard for production.
#
# Note: Users must also provide consent before using these features.
# See Privacy & Terms in the app for details.

# Job Finder uses SerApi to search job listings
# Set to "false" to hide the Find Jobs button (default: enabled)
# VITE_FEATURE_JOB_SEARCH_ENABLED=true

# Web Research uses Tavily for company research
# Set to "false" to disable web research in AI Agent (default: enabled)
# VITE_FEATURE_WEB_RESEARCH_ENABLED=true
