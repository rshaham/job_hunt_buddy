---
description: Run TypeScript type checking without full build
allowed-tools: Bash(npx tsc:*)
---

Run TypeScript type checking to find any type errors:

!`npx tsc --noEmit`

If errors are found, summarize them grouped by file and suggest fixes for each.
